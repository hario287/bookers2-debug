<% if book.favorited_by?(current_user) %>
 <!--いいねがクリックされている状態とされていない状態で表示を変える-->
  <%= link_to book_favorites_path(book), method: :delete do %>
  ♥<%= book.favorites.count %>
  <% end %>
<% else %>
  <%= link_to book_favorites_path(book), method: :post do %>
  ♡<%= book.favorites.count %>
  <% end %>
<% end %>